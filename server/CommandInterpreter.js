
/**
 * Returns whether the given string dictates a command or not
 */
function isCommand(text) {
    return false;
}

/**
 * Runs the given text command
 */
function runCommand(text) {
    //TODO: make
}

module.exports = {
    isCommand: isCommand,
    runCommand: runCommand
};
